import { NgslAsyncHandlerDirective } from './async-handler.directive';

describe('AsyncHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgslAsyncHandlerDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
